{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "get_code",
        "description": "Look up the active access code for a unit. Always start with just the unit_label. If the response is error=needs_phone, ask the caller for their phone number and call again with both unit_label and phone_e164. Open-policy units return the code without auth; restricted-policy units require the caller's phone to be on the rentor's allowlist. Never invent a code.",
        "parameters": {
          "type": "object",
          "properties": {
            "unit_label": {
              "type": "string",
              "description": "The unit's printed label, e.g. 'PR-204' or 'BLUE-7'. Pass exactly as the caller said it; the backend match is case-insensitive."
            },
            "phone_e164": {
              "type": "string",
              "description": "Caller's phone number in E.164 format, e.g. +15555550111. Only required when the first call returns error=needs_phone. Normalize spoken numbers (strip spaces/dashes, add +1 for US if missing)."
            }
          },
          "required": ["unit_label"]
        }
      },
      "server": {
        "url": "https://YOUR_PROJECT_REF.supabase.co/functions/v1/get_code",
        "headers": {
          "x-vapi-secret": "REPLACE_WITH_VAPI_SHARED_SECRET"
        }
      },
      "async": false,
      "messages": [
        {
          "type": "request-start",
          "content": "One moment while I look that up."
        }
      ]
    }
  ]
}
